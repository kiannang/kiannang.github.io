# Blog Archive

This folder preserves the removed blog section.

Original locations:

- `app/blog/`
- `components/share-button.tsx`
- `lib/posts.ts`
- `posts/`
- `public/images/blog/`

The live site no longer links to or builds these routes. `archive/` is excluded from TypeScript checks in the root `tsconfig.json`.
